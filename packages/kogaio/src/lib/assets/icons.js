import { close, errorOutline, iconPlaceholder, ivoryLogo, logout, userPlaceholder } from './images'

export default {
  close,
  errorOutline,
  iconPlaceholder,
  ivoryLogo,
  logout,
  userPlaceholder
}
